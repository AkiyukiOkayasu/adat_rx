[project]
name = "adat_rx"
version = "0.4.1"

[build]
sources = ["src"]
target = { type = "directory", path = "target" }

[test]
simulator = "verilator"
waveform_format = "fst"

[test.verilator]
compile_args = ["-Wno-fatal"]
